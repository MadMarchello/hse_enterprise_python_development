# Homework 01

Проект по дисциплине «Промышленное программирование на Python».

## Запуск

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -e .
pip install -r requirements.txt
python -m homework.main
```

## Тесты

```bash
pytest -q
```

Покрытие модулей `homework` включается само: `--cov` задан в `pyproject.toml` (`addopts`).

### Отключение venv 
```bash
deactivate
```

## Почта

Письмо уходит через Яндекс SMTP (`smtp.yandex.ru:465`). Пароль и адрес отправителя читаются из `.env`, в Git файл не попадает.

```
YANDEX_EMAIL=your_email@yandex.ru
YANDEX_APP_PASSWORD=пароль_приложения
TO_EMAIL=mibelousov@edu.hse.ru
```

Обычный пароль от Яндекса не подойдёт: нужен [пароль приложения](https://id.yandex.ru/security/app-passwords). Если переменных нет, программа спросит их в консоли (пароль — скрытый ввод).



## Зачем `__init__.py`

Файл `src/homework/__init__.py` в этом проекте пустой: логики в нём нет.

Исторически Python считал пакетом только каталог, в котором есть `__init__.py`. Без него `import homework` не работал. С Python 3.3 (PEP 420) каталог может быть namespace package и без этого файла; у нас пакет и так находится через `src` и `pip install -e .`.

Пустой `__init__.py` оставлен как явный маркер пакета и требование структуры задания, а не как часть программы.

## Стабильный вывод `set`

Порядок элементов множества в Python не фиксирован. Без отдельной обработки `str({...})` берёт текущий порядок обхода, и одна и та же программа может дать разный текст отчёта — а значит, разные MD5 и SHA-256.

Поэтому в `_format_value` множество распознаётся через `isinstance(value, set)`, элементы сортируются и только потом собираются в строку.

Для целых чисел разницы часто не видно: хеш `int` не рандомизируется, и `{3, 1, 2}` почти всегда печатается одинаково.

```text
# и со sorted, и с обычным str({3, 1, 2})
{1, 2, 3}
```

Разница проявляется на строках. Без `isinstance` вывод зависит от `PYTHONHASHSEED`:

```text
# PYTHONHASHSEED=0, только str(set)
{'python', 'course', 'homework'}

# PYTHONHASHSEED=42, только str(set)
{'homework', 'course', 'python'}
```

С `isinstance` и сортировкой по `repr` строка всегда одна и та же:

```text
{'course', 'homework', 'python'}
```

## LLM

Архитектуру и тесты помогала собрать модель Grok в Cursor. Ниже промпты, по которым принимались решения. Ответы сверялись с текстом задания, а не копировались целиком.

### Промпт: архитектура

```text
По критериям домашнего задания выбери минимальную архитектуру.

Нужно:
- src-layout: src/homework, tests, README.md, requirements.txt, pyproject.toml, .gitignore
- отдельное окружение .venv, каталог не попадает в Git
- запуск: python -m homework.main и pytest -q
- отчёт по восьми типам: int, float, bool, str, list, tuple, set, dict;
  для каждого значения виден сам объект и type()
- ФИО в начале или в конце одной строки-отчёта
- MD5 и SHA-256 от одного и того же UTF-8 этой строки,
  а не от ФИО и отчёта по отдельности
- письмо с ФИО и двумя хешами; пароль не в исходниках и не в Git
- чистая функция calculate_hashes(text: str) -> tuple[str, str]

Сравни один процедурный модуль, разбиение по ответственности,
классы-сервисы и Clean Architecture.
Оставь вариант, который объясняется на сдаче за пару минут.
Чистые функции (отчёт и хеш) отдели от SMTP и чтения окружения.
Не добавляй слои domain/infrastructure, порты и классы без состояния.
Секреты читай из окружения: .env через python-dotenv, файл в .gitignore.
Если переменных нет, спроси ФИО и адрес в консоли, пароль — через getpass.
```

Из этого сравнения в коде остались четыре модуля и сборка в `main`: `report` собирает строку, `hashing` считает оба дайджеста, `mail` только отправляет письмо, `main` связывает их. Полная слоистая архитектура для одного сценария не нужна.

### Промпт: тесты

```text
Напиши pytest под эту архитектуру. pytest -q не должен открывать SMTP
и не должен читать реальные секреты.

1. calculate_hashes. Эталоны для "hello", пустой строки и "привет".
   Оба хеша — один вызов и одна UTF-8-строка.
   Константы не копируй из вывода самой функции: сверь их внешним
   openssl dgst или shasum. В pipe не должно быть лишнего перевода
   строки: printf '%s', не echo.

2. Отчёт. В выборке есть все восемь типов.
   Текст отчёта стабилен между запусками.
   Для set не используй str(set): порядок зависит от PYTHONHASHSEED.
   Сортируй элементы по repr и проверь множество строк
   {'python', 'course', 'homework'}.

3. Письмо. Подмени smtplib.SMTP_SSL.
   Проверь хост smtp.yandex.ru, порт 465, login и что в теле письма
   есть ФИО, MD5 и SHA-256. С сервером не соединяйся.

4. main. Если FULL_NAME, YANDEX_EMAIL и YANDEX_APP_PASSWORD заданы,
   input и getpass не вызываются.
   Если они пустые, программа спрашивает ФИО, адрес и пароль.
   Хеш считается от текста отчёта, не от ФИО отдельно.
```

### Что проверялось в ответах

- Сигнатура `calculate_hashes` и то, что MD5 и SHA-256 берутся из одной последовательности байтов.
- Эталоны в `tests/test_hashing.py` совпали с `printf '%s' … | openssl dgst` для `hello`, пустой строки и `привет`.
- `pytest -q` не ходит в сеть: SMTP подменён, живое письмо остаётся ручным шагом `python -m homework.main`.
- Пароль не зашит в код: он берётся из `.env` или из `getpass`, а `.env` есть в `.gitignore`.

### Неточность в ответе модели

Первый вариант теста письма делал `msg.get_payload()[0].get_payload(decode=True)`. У `email.message` результат `get_payload()` — это строка или список частей, и проверка типов сообщала: у `str` нет атрибута `get_payload`. Тест переписан: части письма обходятся через `walk()`, тело декодируется только после `isinstance(..., bytes)`.

Там же моки `MagicMock` оставляли предупреждения проверщика типов (`call_args` и методы мока без известного типа). Их заменили на обычные функции и класс `_FakeSMTP` с явными полями.
